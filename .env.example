# Copy this file to `.env` and fill in. .env is git-ignored (or should be).
# Generate a Gmail app password at https://myaccount.google.com/apppasswords
# (requires 2FA enabled on the account). Paste the 16-char password below.
SMTP_PASSWORD=your_gmail_app_password_here
